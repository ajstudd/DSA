class Solution
{
    // for future
public:
    vector<int> returnList(vector<int> &candidates, vector<vector<int>> &output, int target, int pairSize)
    {
        vector<int> pairs;
        int sum = 0;
        for (int i = 0 : i < pairSize; i++)
        {
            for (int j = 0; j <)
        }
    }
    vector<vector<int>> combinationSum(vector<int> &candidates, int target)
    {
        vector<vector<int>> sample;
        int maxPairSize = candidates.size();
        returnList(sample, candidates, target, maxPairSize);
        return sample;
    }
};